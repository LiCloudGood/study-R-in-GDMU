# 项目状态与交接文档

> 这份文件是给「新会话」看的：读完它 + `README.md` + `scripts/原题修正记录.md`，
> 就能在不依赖历史对话上下文的情况下继续工作。

最后更新：见本文件所在提交。

---

## 一、这个仓库是什么

gdmu R 语言课程的在线讲义站点（VitePress），网址
<https://licloudgood.github.io/study-R-in-GDMU/>。

作者：**Li_CloudGood**（课程资料整理）＋ DeepSeek Harness 驱动的 AI 助手（网页实现）。

| 目录 | 作用 | 能否改 |
| --- | --- | --- |
| `资料/原题/` | 学校下发的原始 Word/R/数据 | **仅限错别字、笔误**（作者已授权） |
| `资料/答案/` | 各周完成后的 R 脚本 | 同上 |
| `资料/汇总/` | 各周 HTML（原题+答案合订） | 同上 |
| `docs/intro-it/` 等 | **网站讲义源码**（Markdown） | 可自由改 |
| `docs/.vitepress/` | 站点配置与自定义主题 | 可自由改 |
| `scripts/` | 工具与文档 | 可自由改 |

---

## 二、当前进度

### 已完成

- **14 讲讲义全部完成**（其中 13 讲对应「汇总」里有的 13 周）：
  `intro-it/1-software-install.md` ~ `14-goodness-of-fit-and-contingency.md`
  —— 第 10 讲《初级绘图函数》是后来补的：**汇总和答案两份资料都没有第十周**，
  只有原题和 8 个 starter 脚本，答案与配图是自己跑出来的（见下文「补第十周」）
- 站点结构：首页 / 信息技术基础（14 讲）/ 医学大数据分析与决策（8 讲）/ 卫生统计学（17 章 + 2 个补充专题 + 方法选择器）
- 自定义主题 4 个组件：`ClickAnswer`、`AnswerBlock`、`TrackList`、`ChoiceFlow`
  （`ChoiceFlow` 对应的统计方法选择器**已重新上线**，侧栏位置在「卫生统计学」分组的上方，
  条目名叫「方法选择器」；题库和决策速查表由 `scripts/generate-selector.mjs` 从同一份规则表生成）
- **85 张默认收起的参考答案卡片**、232 个知识点提示、496 处公式、57 张表格
- `资料/原题`/`资料/答案`/`资料/汇总` 的错别字、笔误、地图题、3 处数学性错误已修正
  → 详见 `scripts/原题修正记录.md`
- `资料/汇总` 的 13 份 HTML 已同步修正过的错别字（**PDF 已从仓库移除**，理由见下）
- **已放弃在线运行 R（WebR），改为「预先生成好的静态结果」**：
  - 删掉 `docs/.vitepress/theme/webr.ts` 与 `components/RBlock.vue`
    （`AnswerBlock` 重写为纯静态卡片：代码 + 文字输出 + 输出图形）
  - 第 11 讲 6 张卡片接上 8 张输出图，第 9 讲 9 张卡片接上 15 张输出图
    → `public/figures/09-base-graphics/`、`public/figures/11-ggplot2/`
  - **题面配图也贴回题目下方了**：原题正文里的「图1~图15」（第 9 讲）和
    「图1~图7」（第 11 讲）原先在网站上完全看不到，题目却写着「按图5的样式」，
    学生根本不知道要画成什么样。现已按题号贴回
    → `public/figures/09-question/`、`public/figures/11-question/`
  - 11 张卡片接上 汇总 里跑好的文字输出（只接代码逐句相同的，理由见 README）
  - 构建产物中已确认**不含任何 WebR 代码**

### 《医学大数据分析与决策》已上线

- **8 讲网页讲义**：`docs/Medical-Big-Data-Analysis/`，
  对应《医学大数据分析与决策》第 1~8 周实验，已挂进侧边栏与导航
- 每讲结构：本讲内容 → **装包提醒**（列出当讲用到的 R 包与装法）→
  逐题的「题目 + 题面配图 + 参考答案卡片（代码 / 运行结果 / 输出图形）」→ 运行环境说明
- 素材来自 `资料/医学大数据/`；配图在 `docs/public/figures/mbd/<周>/`
- 生成脚本：`_dev/_gen-lectures.mjs`（未入库）

**R 环境**：本机装了 **R 4.6.1 在 `F:\R\R-4.6.1`**（在仓库外）。
靠它把 42 个答案脚本跑通（39 个完整跑完），运行结果同时喂给网页讲义和汇总。
老师给的 48 个离线 R 包里有 5 个与 R 4.6 的 DLL 不兼容，那几个改从 CRAN 装。

**运行/生成流程**（都在 `_dev/`，未入库）：

1. `run-weeks.ps1` —— 分周执行答案脚本，一周一个 R 会话，产出 `_dev/run/第N周/out.txt` 与 `plots/`
   （`_dev/run/` 是中间产物，已经清掉了；要重跑再生成一遍即可）
2. `_gen-lectures.mjs` —— 生成 8 讲网页讲义
3. `_summary2.mjs` —— 生成 `资料/医学大数据/汇总/` 的 8 份 HTML

> 注意：**不要把生成脚本的 stdout 接 `Select-Object -First N`** ——
> PowerShell 会提前掐断管道，node 进程被杀，后面几周就不生成了。

### 统计方法选择器（已重新上线）

`ChoiceFlow` 那个「统计方法选择器」一度**下线**过，原因是作者认为它的推荐流程
还不够严谨。后来按 `scripts/选择器改造构思.md` 的思路重做了，现在**已经上线**：

- 页面：`docs/Health-statistics/choice.md`（`srcExclude` 已删掉，侧栏里是一条平铺的
  「方法选择器」链接，位于「卫生统计学」分组上方；`docs/Health-statistics/index.md`
  上也有个按钮链过去）
- 题库：`docs/.vitepress/theme/selector-data.ts`（14 个问题节点 + 29 个结论节点）
  —— 组件**直接 import**，不走 `fetch`，避免异步失败时卡在「正在加载」
- 页面上的「决策速查表」和交互问答**出自同一份规则表**，由
  `scripts/generate-selector.mjs` 生成，两者不可能互相打架
- 每个结论都带 `c.link` 指回对应的《卫生统计学》章节，不再是空链接
- 旧的三套题库 `public/tree-{normal,med,expert}.json` 已删除，仓库里不再有它们

**两个生成脚本**（分两步跑，第一步会写出第二步要用的速查表）：

```bash
node scripts/generate-selector.mjs        # 规则表 → theme/selector-data.ts + 决策速查表
node scripts/generate-selector-page.mjs   # 页面正文 + 速查表 → docs/Health-statistics/choice.md
```

> **`choice.md` 是产物，别直接改** —— `generate-selector-page.mjs` 会把它整个覆盖，
> 手改的内容下次一跑就没了。要改页面文案就改那个脚本里的大段模板，再重跑。
> （这一点以前是靠 `_dev/` 里的临时脚本维护的，现在挪进 `scripts/` 了，
> 就是因为「产物在库里、生成器不在库里」迟早会打架。）
>
> 改规则请改 `scripts/generate-selector.mjs` 里的规则表，然后重跑它；
> `selector-data.ts` 同样是产物，标了「请勿手改」。
> 数据结构上有一个坑（规则表用 `m`/数组 `c`，interface 用 `method`/对象 `c`），
> 已在生成脚本的 `normalizeResult()` 处对齐 —— 详见「已解决的问题」第 4 条。
>
> 改完记得跑一次端到端回归测试（会真的开浏览器把 33 条路径全点一遍，
> 外加首页那个入口按钮）：
> `node scripts/test-selector.mjs`（需要先起预览 + 本机有 Edge）

### ⚠️ 自动部署已暂时关闭

.github/workflows/deploy.yml 的触发条件**只剩 workflow_dispatch** ——
推到 main 不会再自动发布到 GitHub Pages，需要手动在 Actions 页面点 Run workflow。

作者要线下审核站点效果，审过之后再恢复自动部署。
**恢复办法**：把 workflow 里注释掉的那两行 on.push.branches 加回来。

### 开发环境要点

- **本机没有 Quarto、没有 R**；PDF 曾靠系统自带的 Microsoft Edge 的 `--print-to-pdf`
  生成（思路见 `scripts/原题修正记录.md` 第六节）
- **13 份 PDF 已从仓库移除**：占 6.6 MB，而 HTML 已经够用，学生直接看网页即可。
  本地备份在 `_dev/汇总-pdf/`（未入库），需要时按上面的办法重新生成
- 依赖：`npm install`；本地预览 `npm run docs:dev`；
  构建 `npm run docs:build`
- **构建需要 esbuild 子进程**：若沙箱禁止 `spawn`，会报 EPERM，需要放宽权限

### 自定义组件速查

`README.md` 现在只面向学生，技术细节挪到这里。四个组件都在
`docs/.vitepress/theme/index.ts` 里全局注册，任意 Markdown 页面直接用，不用 import。

| 组件 | 用途 | 关键属性 |
| --- | --- | --- |
| `<AnswerBlock>` | 参考答案卡片（默认收起） | `code`（必填）、`title`、`description`、`output`、`images`、`defaultOpen` |
| `<ClickAnswer>` | 折叠解答（可放 Markdown / 公式） | `initially-open`、`show-text`、`hide-text`、`theme`、`animation`、`icon-position` |
| `<TrackList>` | 学习进度清单（存 localStorage） | `tasks` |
| `<ChoiceFlow>` | 统计方法选择器（无属性，题库内置） | 题库在 `theme/selector-data.ts`，由 `scripts/generate-selector.mjs` 生成 |

```md
<AnswerBlock
  title="实验题 1 · 参考答案"
  :code="code1101"
  :output="out1101"
  :images="['/figures/11-ggplot2/unnamed-chunk-7-1.png']"
/>
```

坑：

- **不要再加 `:packages` / `:files`**：那是 WebR 时代的属性，现在不生效，
  各讲里遗留的 14 处已经清理掉了。
- **raw HTML 里的图片必须写 `:src="withBase('...')"`**，并在该页
  `<script setup>` 里 `import { withBase } from 'vitepress'`；
  VitePress 只给 Markdown 语法的图片自动补 base，raw HTML 不会。
  `<AnswerBlock>` 的 `images` 属性内部已经自动补好了。
- 自闭合标签批量插属性时容易把 `/>` 劈成两半，插完要 `npm run docs:build` 验一次。

---

## 三、已知问题 / 待办

### A. 已解决的问题（保留记录，避免后人重走弯路）

1. ~~`Cannot convert object to primitive value`~~ —— **随 WebR 一起废弃了**。
   WebR 的 `captureR` 在 R 报错时返回的 `error` 项，其 `data` 是 **RObject** 而不是字符串。
   老代码在 `docs/.vitepress/theme/webr.ts` 里加过 `conditionText()` 做降级处理。
   **现在本站不再在线执行 R，这个坑不存在了。**

2. ~~绘图没有显示出来~~ —— **同上，已废弃**。
   现在图是预先跑好、直接放在 `public/figures/` 下的静态图片。

3. ~~脚本里的「错误」会中断后续代码~~ —— **这正是放弃 WebR 的主要原因之一**。
   例如 `test0501.R` 的 `dts - 30`（题目本来就要求它报错）、
   `test0601.R` 的 `object 'num' not found`、`test0801.R` 的 `count_even()`，
   这些报错是**题目的考点**，但在 WebR 里一旦报错后面的代码就不再执行，
   反而破坏了教学效果。现在结果都是静态的，不受影响。

4. ~~选择题选完之后不出答案（一片空白）~~ —— **已修**。
   现象：一路点到最后，结果块**什么都不显示**，也不报错。
   原因：题库生成脚本在写 `selector-data.ts` 时把规则表原样 `JSON.stringify` 出去，
   用的是简写 `m`（方法名）和 `c`（`[章名, 链接]` **数组**）；
   而同一个脚本里手写的 `interface SelectorResult` 声明的是
   `method: string` 和 `c: { text, link }`，组件又是照着 interface 写的。
   于是组件取 `result.c.link` 得到 `undefined`，
   传给 VitePress 的 `withBase()` 时命中它内部的 `path.startsWith('/')`，
   **抛 TypeError**；模板里抛错会让整块结果渲染失败，所以页面上只剩空白。
   修法（两处，缺一不可）：
   - 生成脚本加 `normalizeResult()`，在唯一出口 `pushResult()` 处把简写统一成长名字，
     数据和 interface 从此不会各说各话；
   - 组件里把章节链接改成 computed + 兜底默认值，并加一个 `v-else` 分支 ——
     万一以后又出现对不上的情况，页面会明说「这一步没有找到对应的结论」，
     而不是让用户对着空白猜。
   **教训**：上一轮只验证了「页面能渲染出第一道题」就以为修好了，
   漏掉的是「一路点到底」这条路。现在 `_dev/sel-click.mjs` 会用真实浏览器
   把题库里全部 33 条路径逐条点一遍，核对方法名、章节链接和控制台报错。

### B. 作者提过的想法

- **把「在线运行」换成「预先生成的静态结果」** —— **已实施**（见上文「已完成」）。
  图片直接取自 `资料/汇总/第N周——xxx_files/figure-html/`，
  也就是作者当年用 Quarto 渲染出来的原始配图，**不需要跑 R**。
  第 11 讲按文件名里的 chunk 序号 7~12 与实验题 1~6 一一对应（已用源码内容核对过）。

  > 注意：文字输出的覆盖率只有 11/85。**不是漏做**——
  > 讲义里不少答案代码是重写的解法，和 汇总 源码逐句比对不一致时，
  > 贴 汇总 的输出会导致「结果与代码对不上」，宁可不贴。
  > 比对脚本思路见 `_extract-outs.mjs` / `_outdiff.mjs`（未纳入版本管理）。

- **第十周「初级绘图函数」不在网站上**：`资料/原题/第十周原题` 有 docx 和 8 个脚本，
  但**没有答案、也没有汇总**。若补成第 14 讲，需要给 ggplot2 之后的 4 讲顺号
  （URL 会变）。
- **不做**：把「原题+答案」额外导出成 Markdown。
  理由：已有 docx / HTML 两份副本，站点讲义本身就是更适合学生读的
  Markdown，再导一份只会增加不同步的风险。

---

## 四、给下一个会话的建议

1. 先读 `README.md`、`scripts/原题修正记录.md` 和本文件，再动手。
2. 改动 `资料/原题`/`资料/答案`/`资料/汇总` 前，务必先看清楚「一、这个仓库是什么」里的授权范围。
3. 改 `.docx` 用脚本做定点替换（思路：把 `<w:t>` 拼成段落文本后定位再映射回片段，
   公式里的 `<m:t>` 要按「第 N 个 `<m:oMath>`」限定范围），改完**必须做结构校验**：
   除 `word/document.xml` 外其余条目逐字节一致。
4. 本地预览：`npm run docs:dev`，地址
   <http://127.0.0.1:5173/study-R-in-GDMU/>。
5. 改完一定要跑一次 `npm run docs:build`，并检查三件事：
   - 内链有没有 404（`/intro-it/` 首页的课程卡片曾经因为 raw HTML 的 `:href`
     没走 `withBase()` 而全部 404，用 `withBase()` 才修好）
   - `public/figures/` 下的图有没有被复制进 `dist`
   - 构建产物里有没有 WebR 残留（应该为 0）
6. **推送前先问作者**：本轮所有提交都还在本地，未 push。
