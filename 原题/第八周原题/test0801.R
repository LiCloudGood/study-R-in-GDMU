# 在下面空白定义函数count_even,
# 然后统计向量x中偶数个数








# 运行下面的代码，
# 观察结果是否满足题目要求
x = round(runif(20,1,100),digits = 0)
print(x)
y = 20
z = c(FALSE, TRUE, FALSE, TRUE, TRUE)
w = matrix(1:12,nrow = 3)

count_even(x)
count_even()
count_even(y)
count_even(z)